/* CREATE TABLE public.users
CREATE TABLE users (
                       id serial NOT NULL,
                       fullname character varying(1000),
                       forsage_id bigint,
                       reference_link character varying(1000),
                       slotx3 bigint,
                       slotx4 bigint,
                       value integer,
                       left_user character varying(3000),
                       right_user character varying(3000),
                       phone character varying(100),
                       PRIMARY KEY (id)
)*/